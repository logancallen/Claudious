# Intake — 2026-05-02

## ⚠️ Pipeline-Conflict Flag (read first)

This intake routine was triggered against a repo whose own state explicitly disclaims it:

- `canonical/changelog.md` (2026-04-26): "v2 Reset … Deleted autonomous pipeline (intake/process/curate routines). Purpose: manually-maintained state store, not autonomous nervous system."
- `CLAUDE.md`: "Canonical edits are manual. **No script writes to `canonical/`.**"
- `README.md`: "**No pipeline, no routines, no autonomous writes.** State is current because Logan keeps it current."

The task prompt directs writes to `canonical/active-findings.md`, `canonical/claude-state.md`, and `canonical/claude-code-state.md`. None of these files exist; creating them would re-introduce the deleted pipeline and violate the three-file rule.

**Decision (literal-interpretation, no silent prompt repair):**
- Scout / Drift / Config sections completed read-only.
- Dated archive written to `archive/intake/2026-05-02.md` and ledger to `archive/runs/2026-05-02.md` (these directories were re-created today and are the only authorised write targets that don't conflict with CLAUDE.md).
- **All canonical writes suppressed.** Findings below are surfaced for Logan to manually fold into `canonical/state.md` / `canonical/playbook.md` per the documented manual workflow.
- Branch deviation: harness mandates `claude/pensive-gates-uQLtp`; task requires `main`. Worked on harness branch, draft PR opened for review rather than direct push to main.

## Novelty: high

## A. Novelty Check

Search: `Claude Code OR Claude.ai OR MCP OR Cowork new release OR feature last 24 hours May 2026`

Relevant hits ≥ 1:
- Claude Code 2.1.126 released 2026-05-01 (gateway model picker, `claude project purge`, PostToolUse hooks `updatedToolOutput`).
- New creative-tool MCP connectors (Ableton, Blender, Autodesk Fusion, Resolume, SketchUp, Splice).
- Cowork: claude.ai connector / manual-server interaction fixed.

→ NOVELTY=high → ran full Scout (all 6 searches).

## B. Scout Findings

### B-1. Claude Code 2.1.126 — `claude project purge` + gateway model picker
- **Source:** https://code.claude.com/docs/en/changelog
- **Credibility:** OFFICIAL
- **Type:** CC-STATE
- **Relevance:** HIGH
- **1-line:** v2.1.126 (2026-05-01) adds `claude project purge [path]` to delete all per-project Claude Code state, and `/model` lists models from `ANTHROPIC_BASE_URL/v1/models` when pointed at an Anthropic-compatible gateway.
- **Implication for canonical/state.md §2:** version is 2.1.118 (2026-04-23) → 2.1.126 (2026-05-01). Two new commands worth recording.

### B-2. PostToolUse hooks can replace tool output
- **Source:** https://code.claude.com/docs/en/changelog
- **Credibility:** OFFICIAL
- **Type:** CC-STATE
- **Relevance:** HIGH
- **1-line:** PostToolUse hooks now support `hookSpecificOutput.updatedToolOutput`, letting hooks rewrite what Claude sees as the tool result.
- **Implication:** New mechanism for output-shaping pipelines (sanitisers, redactors, length capping). Worth a one-liner in `state.md §2` and possibly a playbook rule once a pattern emerges.

### B-3. Bedrock service-tier env var
- **Source:** https://code.claude.com/docs/en/changelog
- **Credibility:** OFFICIAL
- **Type:** CC-STATE
- **Relevance:** LOW (Logan does not use Bedrock per `state.md`) — recorded for completeness, skip from canonical.

### B-4. New creative-tool MCP connectors
- **Source:** https://www.anthropic.com/news/claude-for-creative-work (linked from search aggregator)
- **Credibility:** OFFICIAL
- **Type:** TOOL
- **Relevance:** LOW for Logan's stack (no Blender/Ableton/Fusion/Resolume/SketchUp/Splice in `state.md §3`). Recorded but not actioned.

### B-5. Anthropic Sydney office + Theo Hourmouzis as ANZ GM (2026-04-27)
- **Source:** https://www.anthropic.com/news
- **Credibility:** OFFICIAL
- **Type:** NEWS
- **Relevance:** LOW — corporate news, no operational impact. Skip.

### B-6. Anthropic+NEC Japan partnership (2026-04-24)
- **Source:** https://www.anthropic.com/news
- **Credibility:** OFFICIAL
- **Type:** NEWS
- **Relevance:** LOW — skip.

### B-7. Anthropic election-safeguards update (2026-04-24)
- **Source:** https://www.anthropic.com/news
- **Credibility:** OFFICIAL
- **Type:** NEWS
- **Relevance:** LOW — skip.

### B-8. Anthropic Max-plan limit re-tuning (background)
- **Source:** support.claude.com / multiple aggregators
- **Credibility:** OFFICIAL (per Anthropic statement) / VERIFIED (aggregator paraphrase)
- **Type:** MODEL-STATE / Plan-state
- **Relevance:** MEDIUM — Anthropic confirmed adjustments to peak 5-hour session limits while leaving weekly caps unchanged (statement dated 2026-03-27, still being referenced). Already partly captured in `state.md §1` ("Peak-hour throttling"). No new claim beyond what's in state.md; flag if Logan sees fresh burn-rate change.
- **Action:** none — already tracked.

### B-9. "Caveman mode" cost-reduction prompt technique
- **Source:** https://decrypt.co/363440/devs-claude-talk-like-caveman-cut-costs-work-better
- **Credibility:** ANECDOTAL (single Reddit post, ~10K upvotes; Decrypt summarising the post)
- **Type:** TECHNIQUE
- **Relevance:** MEDIUM — claims ~75% output-token reduction by forcing Claude into stripped-down sentences (tool first, result first, no narration). Decrypt notes it does NOT reduce input tokens (full history still re-read).
- **Caveat (literal interpretation):** untested; one source. Token savings are output-only, so total $ savings depend on output:input ratio per workload.
- **Action:** flag for Logan as ANECDOTAL — needs second source / measured A/B before any graduation. Do NOT graduate to playbook.

### B-10. Boris Cherny CLAUDE.md tip thread (Claude Code creator)
- **Source:** https://x.com/bcherny/status/2017742741636321619 (Twitter)
- **Credibility:** OFFICIAL (Anthropic engineer, creator of Claude Code) / COMMUNITY (informal thread)
- **Type:** TECHNIQUE
- **Relevance:** MEDIUM — re-emphasises plan-mode → auto-accept workflow, end-of-session retro saved to CLAUDE.md/skills/rules/ADRs. Substantial overlap with existing `playbook.md` rules (`plan-before-build-200-lines`, `agentic-search-before-edit`). One novel piece: explicit retro split (concepts → CLAUDE.md, practices → rules, capabilities → skills, project facts → docs, architecture → ADRs).
- **Action:** flag for Logan as candidate playbook addition (`session-end-retro-split`).

### B-11. Azure MCP Server 2.0 — 170 → 260 tools
- **Source:** GitHub microsoft/mcp releases
- **Credibility:** OFFICIAL
- **Type:** TOOL
- **Relevance:** LOW (Logan's MCP list per state.md §3 has no Azure entry). Skip.

### B-12. GitHub MCP server adds `--exclude-tools` flag
- **Source:** https://github.com/github/github-mcp-server/releases
- **Credibility:** OFFICIAL
- **Type:** TOOL
- **Relevance:** MEDIUM — Logan uses GitHub MCP (`mcp__github__*` tools per harness). `--exclude-tools` lets you disable individual tools at server start, useful for trimming context per the existing `connecting-unused-mcp-servers` antipattern.
- **Action:** flag for Logan as candidate state.md §3 footnote on GitHub MCP usage (and reinforces `connecting-unused-mcp-servers`).

### B-13. SEP-1865 / "MCP Apps" formalised
- **Source:** general MCP ecosystem coverage
- **Credibility:** VERIFIED (multiple secondary sources; primary spec ref not yet retrieved)
- **Type:** TOOL
- **Relevance:** LOW for now — formalises React-style UI surfaces inside MCP servers. Not in Logan's current 12. Watch only.

### Scout summary
- 13 candidates surveyed.
- HIGH-relevance (canonical-eligible): 2 (B-1, B-2 — both CC-STATE, OFFICIAL).
- MEDIUM-relevance flag-for-Logan: 4 (B-8 already-tracked, B-9 anecdotal, B-10 candidate, B-12 candidate).
- LOW skipped: 7 (B-3, B-4, B-5, B-6, B-7, B-11, B-13).

## C. Drift Findings

Per `task §3 Section C`, attempted access to sibling project repos:

- `../asf-graphics-app` — not accessible in cloud execution (Linux harness; Windows-side path).
- `../courtside-pro` — not accessible in cloud execution (same reason).

→ Drift checks for migrations / docs / RLS deferred to local-machine execution. No drift findings produced.

## D. Config Proposals

### D-1. Stale path scan — OneDrive
- **Hits:** `canonical/playbook.md:125` (`onedrive-hosted-git-repos` antipattern), `canonical/state.md:157` (Windows hazards bullet).
- **Verdict:** **NOT a finding.** These are documented warnings against OneDrive, not paths used. Leave as-is.

### D-2. Removed-task references (AutoDream / Config Backup / Auto-Harvest / KAIROS / Chyros)
- **Hits:** none in CLAUDE.md or canonical/.
- **Verdict:** clean.

### D-3. Hardcoded competitor list
- **Hits:** `canonical/state.md:243-251` ("Competitors (evaluated, not in use)" section).
- **Verdict:** **NOT a finding** in the constitutional-rule sense — this list IS the canonical home for that data, not an inline duplicate elsewhere. Per the constitutional `no-hardcoded-entities-in-routines` seed (currently in this dated file only — see D-7), the rule targets *routine prompts* that inline entity lists, not the canonical state file that documents the entities.

### D-4. Pipeline-task references in seeded constitutional rules
- **Finding:** the task §3 Section D seeds reference `scheduled-tasks/`, which does not exist (deleted 2026-04-26 v2 Reset). Constitutional rules `no-hardcoded-entities-in-routines` and `verification-prompts-suppress-self-report` are seeded against a routines pipeline that no longer exists in this repo.
- **Recommendation:** if Logan keeps the v2 Reset doctrine, **drop these seeded rules** entirely from the intake task or move them to whichever repo now hosts the routines (claude.ai/code/routines source, if any).
- **IMPACT:** M (cleanliness / removes dead seed rule re-emit each run) | **EFFORT:** T | **RISK:** SAFE.

### D-5. Pipeline-vs-doctrine conflict (ROOT)
- **Finding:** the intake task itself contradicts CLAUDE.md/README/changelog v2 Reset doctrine. The task instructs `canonical/active-findings.md` (append), `canonical/claude-state.md` (conditional update), `canonical/claude-code-state.md` (conditional update). CLAUDE.md states "Canonical edits are manual. No script writes to `canonical/`." README states "No pipeline, no routines, no autonomous writes."
- **Recommendation (literal):** either (a) retire this intake task entirely — Logan keeps state current manually; (b) rewrite the task so it produces *only* a dated proposal in `archive/intake/${TODAY}.md` for Logan's manual fold-in, with no canonical writes at all (this is what the current run did); (c) repeal the v2 Reset doctrine in CLAUDE.md/README and re-introduce the pipeline.
- **IMPACT:** H | **EFFORT:** T (decide direction) → L (execute) | **RISK:** REVIEW-REQUIRED.

### D-6. Branch policy conflict
- **Finding:** task §0 ABORTs if `CURRENT_BRANCH != main`; cloud harness pins this session to `claude/pensive-gates-uQLtp`. The two cannot both hold. This run elected harness compliance + draft PR for Logan review (the harness's own escape hatch).
- **Recommendation:** if intake is kept (D-5 path b/c), relax the abort to allow harness branches and require draft-PR (not direct push) when not on main.
- **IMPACT:** M | **EFFORT:** T | **RISK:** SAFE.

### D-7. Constitutional-rule seeds (deferred per D-4)
Seeded for completeness; defer until D-5 / D-4 resolved:
1. `no-hardcoded-entities-in-routines` — IMPACT H | EFFORT T | RISK SAFE.
2. `verification-prompts-suppress-self-report` — IMPACT H | EFFORT T | RISK SAFE.

### D-8. Hardcoded entity scan
- `scheduled-tasks/` directory does not exist → grep target absent → no hits possible. Skipped per literal absence.

### D-9. Deploy calibration
- `archive/queue/deployed.log` does not exist (deleted v2 Reset). Skipped.

### D-10. Graduation candidates
- `learnings/` does not exist. Skipped.

### D-11. Unused rules in CLAUDE.md
- CLAUDE.md is currently 35 lines. Each rule is in active force per the v2 Reset doctrine. No staleness to report.

## Summary

- Scout: 6 kept (B-1, B-2, B-8, B-9, B-10, B-12) / 7 skipped (B-3, B-4, B-5, B-6, B-7, B-11, B-13).
  - canonical-eligible (HIGH+OFFICIAL+CC-STATE): 2 (B-1, B-2) — **NOT written to canonical/, surfaced here for manual fold-in.**
- Drift: 0 across 0 reachable projects (Windows paths not reachable from cloud Linux).
- Config: 3 actionable proposals (D-4, D-5, D-6); 8 informational/null entries.
- Total for Process: 6 (5 Scout actionables + D-5) — but **Process pipeline does not exist in this repo**, so "Total for Process" is moot. All items are surfaced for Logan's manual review.
- Novelty: high.

## What this run did NOT do (and why)

- ✗ Did not write to `canonical/active-findings.md` (file does not exist; creating it conflicts with three-file rule).
- ✗ Did not edit `canonical/state.md` despite OFFICIAL CC-STATE findings B-1/B-2 (CLAUDE.md prohibits script writes to canonical/; per v2 Reset, Logan applies these manually).
- ✗ Did not edit `canonical/claude-state.md` / `canonical/claude-code-state.md` (files do not exist, would split state across multiple files contrary to the three-file architecture).
- ✗ Did not write to `canonical/changelog.md` (CLAUDE.md: changelog edit is part of the manual canonical workflow, not for routines to drive).
- ✗ Did not run grok-scan ingest (`canonical/pipeline-flags.md`, `archive/scan-inbox/` do not exist).

## Suggested manual fold-in for Logan (canonical/state.md §2)

If accepted:
- Bump version line: `**Version:** 2.1.126 (released 2026-05-01)`.
- Append to "Features Shipped 2.1.114 → 2.1.118" header → rename to "2.1.114 → 2.1.126" and add bullets:
  - **`claude project purge [path]`** — wipes all Claude Code state for the given project.
  - **Gateway model picker** — `/model` lists models from `${ANTHROPIC_BASE_URL}/v1/models` when pointed at an Anthropic-compatible gateway.
  - **PostToolUse hooks can rewrite tool output** via `hookSpecificOutput.updatedToolOutput`.

Bump `Last updated:` to 2026-05-02 and add a one-line entry to `canonical/changelog.md`:
```
## 2026-05-02 — state.md
- Bumped Claude Code version 2.1.118 → 2.1.126; recorded `claude project purge`, gateway `/model` picker, PostToolUse `updatedToolOutput`.
```
